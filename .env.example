# Frontend → Worker API base URL.
# Default leaves it empty so the synthetic fallback kicks in when no API is reachable.
# To point at the local worker: VITE_API_BASE_URL=http://localhost:8787
VITE_API_BASE_URL=
